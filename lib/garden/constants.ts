/**
 * Мир клумбы измеряется в «единицах мира». Координаты цветов в базе хранятся
 * нормализованными (0..1) и умножаются на WORLD_W / WORLD_H.
 * Если меняете размеры — поменяйте и генератор слотов в supabase/migrations/0004_seed.sql.
 */
export const WORLD_W = 2000;
export const WORLD_H = 1100;

/** Овал газона. */
export const LAWN = { cx: 1000, cy: 570, rx: 960, ry: 470 };

/** Символ школы — подсолнух ОК10 в центре клумбы (низ стебля в точке x, baseY). */
export const EMBLEM = { x: 1000, baseY: 566, height: 236 };

/** Зона, где цветы не сажаются (вокруг головы подсолнуха). Совпадает с SQL-генератором. */
export const EMBLEM_HOLE = { cx: 1000, cy: 448, rx: 122, ry: 128 };

/** Длительность анимации посадки, мс. */
export const GROW_MS = 1500;
